# Exercise 6-7 – Character Count

## Objective

Write a Java method named `count` that accepts two arguments:

- A `String` value.
- A `char` value.

The method should return the total number of times the specified character appears in the string.

---

## Problem Statement

The program should implement a method with the following form:

```java
public static int count(String s, char c)