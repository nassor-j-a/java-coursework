# Exercise 10-3 – Union and Intersection

## Objective

Write Java methods `union` and `intersection` that, given two arrays of integers, print the union and intersection of the two arrays.

The exercise assumes that the arrays do not contain duplicate values.

---

## Problem Statement

Given two arrays of integers:

```text
Array1 = 12, 32, 14, 35, 89, 16, 120
Array2 = 9, 12, 8, 17, 120, 35, 36